#include <stdio.h>

/**
 * main - entry point
 * and prints the last digit of the number in the variable n
 * Return: 0 Always (Success)
 */
int main(void)
{
	int n;
	for (n = 0; n < 10; n++)
	{
	printf("%d", n);
	}
	printf("\n");
	return (0);
}
